# Руководство по использованию CLI модулей

## 📋 Содержание
1. [Модуль cli_text.py](#модуль-cli_textpy)
2. [Модуль cli_convert.py](#модуль-cli_convertpy)
3. [Примеры использования](#примеры-использования)

---

## Модуль `cli_text.py`

### Команда `stats` - Анализ частот слов

**Синтаксис:**
```bash
python src/lab06/cli_text.py stats --input <путь_к_файлу> [--top N]
```

**Параметры:**
- `--input` (обязательный) - путь к текстовому файлу
- `--top` (опциональный) - количество топ слов для вывода (по умолчанию: 5)

**Примеры:**
```bash
# Анализ файла с топ-5 словами (по умолчанию)
python src/lab06/cli_text.py stats --input src/Data/input.txt

# Анализ с топ-10 словами
python src/lab06/cli_text.py stats --input src/Data/input.txt --top 10

# Анализ другого файла
python src/lab06/cli_text.py stats --input src/Data/input_test.txt --top 3
```

**Вывод:**
```
Всего слов: 150
Уникальных слов: 45
Топ-5:
  слово1: 12
  слово2: 8
  слово3: 6
  слово4: 5
  слово5: 4
```

---

### Команда `cat` - Вывод содержимого файла

**Синтаксис:**
```bash
python src/lab06/cli_text.py cat --input <путь_к_файлу> [-n]
```

**Параметры:**
- `--input` (обязательный) - путь к файлу
- `-n` (опциональный) - нумерация строк

**Примеры:**
```bash
# Обычный вывод без нумерации
python src/lab06/cli_text.py cat --input src/Data/input.txt

# Вывод с нумерацией строк
python src/lab06/cli_text.py cat --input src/Data/input.txt -n

# Вывод другого файла с нумерацией
python src/lab06/cli_text.py cat --input src/Data/input_test.txt -n
```

**Вывод (с -n):**
```
     1  Первая строка файла
     2  Вторая строка файла
     3  Третья строка файла
```

---

### Справка по модулю

```bash
# Общая справка
python src/lab06/cli_text.py --help

# Справка по команде stats
python src/lab06/cli_text.py stats --help

# Справка по команде cat
python src/lab06/cli_text.py cat --help
```

---

## Модуль `cli_convert.py`

### Команда `json2csv` - Конвертация JSON → CSV

**Синтаксис:**
```bash
python src/lab06/cli_convert.py json2csv --in <входной_json> --out <выходной_csv>
```

**Параметры:**
- `--in` (обязательный) - путь к входному JSON файлу
- `--out` (обязательный) - путь к выходному CSV файлу

**Пример:**
```bash
python src/lab06/cli_convert.py json2csv --in src/Data/test1.json --out src/Data/output.csv
```

---

### Команда `csv2json` - Конвертация CSV → JSON

**Синтаксис:**
```bash
python src/lab06/cli_convert.py csv2json --in <входной_csv> --out <выходной_json>
```

**Параметры:**
- `--in` (обязательный) - путь к входному CSV файлу
- `--out` (обязательный) - путь к выходному JSON файлу

**Примеры:**
```bash
# Конвертация существующего CSV файла
python src/lab06/cli_convert.py csv2json --in src/Data/test.csv --out src/Data/output.json

# Конвертация другого файла
python src/lab06/cli_convert.py csv2json --in src/Data/test1.csv --out src/Data/test1_output.json
```

---

### Команда `csv2xlsx` - Конвертация CSV → XLSX

**Синтаксис:**
```bash
python src/lab06/cli_convert.py csv2xlsx --in <входной_csv> --out <выходной_xlsx>
```

**Параметры:**
- `--in` (обязательный) - путь к входному CSV файлу
- `--out` (обязательный) - путь к выходному XLSX файлу

**Примеры:**
```bash
# Конвертация CSV в Excel
python src/lab06/cli_convert.py csv2xlsx --in src/Data/test.csv --out src/Data/output.xlsx

# Конвертация другого файла
python src/lab06/cli_convert.py csv2xlsx --in src/Data/test1.csv --out src/Data/test1_output.xlsx
```

---

### Справка по модулю

```bash
# Общая справка
python src/lab06/cli_convert.py --help

# Справка по команде json2csv
python src/lab06/cli_convert.py json2csv --help

# Справка по команде csv2json
python src/lab06/cli_convert.py csv2json --help

# Справка по команде csv2xlsx
python src/lab06/cli_convert.py csv2xlsx --help
```

---

## Примеры использования

### Пример 1: Анализ текста
```bash
# 1. Проанализировать частоты слов в файле
python src/lab06/cli_text.py stats --input src/Data/input.txt --top 10

# 2. Показать содержимое файла с нумерацией
python src/lab06/cli_text.py cat --input src/Data/input.txt -n
```

### Пример 2: Конвертация файлов
```bash
# 1. JSON → CSV
python src/lab06/cli_convert.py json2csv --in src/Data/test1.json --out src/Data/result.csv

# 2. CSV → JSON
python src/lab06/cli_convert.py csv2json --in src/Data/test.csv --out src/Data/result.json

# 3. CSV → XLSX
python src/lab06/cli_convert.py csv2xlsx --in src/Data/test.csv --out src/Data/result.xlsx
```

### Пример 3: Полный workflow
```bash
# 1. Посмотреть содержимое текстового файла
python src/lab06/cli_text.py cat --input src/Data/input.txt -n

# 2. Проанализировать частоты слов
python src/lab06/cli_text.py stats --input src/Data/input.txt --top 5

# 3. Конвертировать JSON в CSV для дальнейшей обработки
python src/lab06/cli_convert.py json2csv --in src/Data/test1.json --out src/Data/processed.csv

# 4. Конвертировать CSV в Excel для просмотра
python src/lab06/cli_convert.py csv2xlsx --in src/Data/processed.csv --out src/Data/processed.xlsx
```

---

## Обработка ошибок

### Файл не найден
Если указанный файл не существует, вы получите понятное сообщение:
```
error: Файл не найден: src/Data/nonexistent.txt
```

### Неверные аргументы
Если не указаны обязательные параметры:
```
error: Необходимо указать команду (stats или cat)
error: the following arguments are required: --input
```

### Все сообщения на русском языке
Все ошибки выводятся на русском языке с понятными описаниями.

---

## Примечания

1. **Пути к файлам**: Можно использовать как относительные (от корня проекта), так и абсолютные пути
2. **Кодировка**: Все файлы читаются в кодировке UTF-8
3. **Зависимости**: Модули используют функции из `lab03` и `lab05`, убедитесь, что эти папки существуют
4. **Справка**: Всегда можно получить справку с помощью `--help`

---

## Быстрый старт

```bash
# Проверка работы модулей
python src/lab06/cli_text.py --help
python src/lab06/cli_convert.py --help

# Тестовый запуск
python src/lab06/cli_text.py stats --input src/Data/input.txt
python src/lab06/cli_text.py cat --input src/Data/input.txt -n
```


